[pytest]
asyncio_mode = auto
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests requiring hardware or real services
    security: marks path-safety and auth-related tests
